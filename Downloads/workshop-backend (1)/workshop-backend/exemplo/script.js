console.log ("salve, quebrada")

var entrada = documnt.querySelector("input")
var saida = document.querySelector("resposta ano")

function cliquei (){
    var ano = entrada.value
    console.log(ano)
    saida.texContent = ano
}